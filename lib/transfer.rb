require_relative 'bank_account.rb'

class Transfer
  #def initialize - sender, receiver, xfer amount, status = "pending" 
  #end
  #valid? return true if account is valid , calls on sender/receiver's valid methods
  #execute_transaction - create a successful transaction status that makes status complete
    #a transaction greater than one == false
  #reverse_transfer - it undoes a transaction between two accounts
    #it depends on executed_transfers
end
